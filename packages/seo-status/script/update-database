#!/usr/bin/env bash

cd $( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
cd ../

rm -rf migrations/*

php bin/console make:migration --no-interaction  --quiet
php bin/console doctrine:migrations:migrate --no-interaction --quiet
# Action to normalize a column before setting it not nullable and re-run this input